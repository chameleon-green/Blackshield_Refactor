//draw_self();
//draw_text(x,bbox_top,string(id));
/*
var LOSList = ds_list_create();
			ds_list_read(LOSList,nodes_in_los(4000,o_platform,o_navnode,x,y,-1));
			var LOSListSize = ds_list_size(LOSList);
			var i;
			
			//find the cost of each node
			for(i = 0; i < LOSListSize; i++){		
				var NodeArray = 0;
				var NodeID = LOSList[| i];
				draw_line(x,y,NodeID.x,NodeID.y);	
			};
ds_list_destroy(LOSList);




draw_self();
//draw_text(x,bbox_top,id);
//draw_line(x,y,creator.x,creator.y);

/*
draw_rectangle(x-90,y,x+90,y+900,1)

